'use strict';

const React = require('react');

const NavBar = React.createClass({
  render:function() {
    return (
      <div className="navBar">
        <h1>Sketchbook</h1>
      </div>
    );
  }
});

module.exports = NavBar;
